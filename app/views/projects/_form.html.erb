<%= simple_form_for @project do |f| %>
  <%= render 'shared/projects_error_messages' %>
<label>Name of your project</label>
  <%= f.text_field :name %>
<!-- <label>Description</label> -->
  <%= f.input :description %>
  <style>
  textarea {
    width: 100%;
    height: 150px;
    padding: 12px 20px;
    box-sizing: border-box;
    border: 2px solid #3F516B;
    border-radius: 4px;
    background-color: #3F516B;
    font-size: 16px;
    resize: none;
  }
  </style>
  <br/>

  <%= f.select :progress, options_for_select(["Idea", "In Progress", "Completed"]), prompt: "-select-"  %>

  <%= f.submit %>
<% end %>
