<%= simple_form_for ([@project, @project.tasks.build]) do |f| %>
  <%= render 'shared/projects_error_messages' %>
  <%= f.input :title %>
  <%= f.input :description %>
  <%= f.select :status, options_for_select([nil, "todo", "inProg", "recent"]) %>
  <%= f.submit %>
<% end %>
